---
- hosts: all
  gather_facts: smart
  become: yes
  pre_tasks:
    - name: 'install python'
      raw: sudo apt -y update && apt-get -y install python

  roles:
    - role: lvm
      task_name: create_lvm
